overwrite: true
schema: "http://localhost:5000/graphql"
documents: "src/**/*.graphql"
generates:
  src/generated:
    preset: "client"
    plugins: []
  src/generated/graphql.schema.json:
    plugins:
      - "introspection"
